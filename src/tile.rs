#[derive(Debug, Clone)]
#[derive(PartialEq)]
pub enum TileType {
    Forest,
    Mountain,
    Lake,
    Village,
    Dungeon,
}


#[derive(Debug, Clone)]
pub struct Treasure {
    // Add treasure properties
}

